module.exports = {
  async Search(search) {
    let amount = 10;
    try {
      let yts = require('yt-search');
      let { videos } = await yts(search);
      console.log(videos[0].url);
      return videos.splice(0, amount);
    } catch (e) {
      try {
        let yts = require('yt-search');
        let { videos } = await yts(search);
        console.log(videos[0].url);
        return videos.splice(0, amount);
      } catch (e) {
        try {
          let yts = require('yt-search');
          let { videos } = await yts(search);
          console.log(videos[0].url);
          return videos.splice(0, amount);
        } catch (e) {
          try {
            let yts = require('yt-search');
            let { videos } = await yts(search);
            console.log(videos[0].url);
            return videos.splice(0, amount);
          } catch (e) {
            try {
              let yts = require('yt-search');
              let { videos } = await yts(search);
              console.log(videos[0].url);
              return videos.splice(0, amount);
            } catch (e) {
              try {
                let yts = require('yt-search');
                let { videos } = await yts(search);
                console.log(videos[0].url);
                return videos.splice(0, amount);
              } catch (e) {
                try {
                  let yts = require('yt-search');
                  let { videos } = await yts(search);
                  console.log(videos[0].url);
                  return videos.splice(0, amount);
                } catch (e) {
                  try {
                    let yts = require('yt-search');
                    let { videos } = await yts(search);
                    console.log(videos[0].url);
                    return videos.splice(0, amount);
                  } catch (e) {
                    try {
                      let yts = require('yt-search');
                      let { videos } = await yts(search);
                      console.log(videos[0].url);
                      return videos.splice(0, amount);
                    } catch (e) {
                      try {
                        let yts = require('yt-search');
                        let { videos } = await yts(search);
                        console.log(videos[0].url);
                        return videos.splice(0, amount);
                      } catch (e) {
                        try {
                          let yts = require('yt-search');
                          let { videos } = await yts(search);
                          console.log(videos[0].url);
                          return videos.splice(0, amount);
                        } catch (e) {
                          try {
                            let yts = require('yt-search');
                            let { videos } = await yts(search);
                            console.log(videos[0].url);
                            return videos.splice(0, amount);
                          } catch (e) {
                            try {
                              let yts = require('yt-search');
                              let { videos } = await yts(search);
                              console.log(videos[0].url);
                              return videos.splice(0, amount);
                            } catch (e) {
                              try {
                                let yts = require('yt-search');
                                let { videos } = await yts(search);
                                console.log(videos[0].url);
                                return videos.splice(0, amount);
                              } catch (e) {
                                try {
                                  let yts = require('yt-search');
                                  let { videos } = await yts(search);
                                  console.log(videos[0].url);
                                  return videos.splice(0, amount);
                                } catch (e) {
                                  try {
                                    let yts = require('yt-search');
                                    let { videos } = await yts(search);
                                    console.log(videos[0].url);
                                    return videos.splice(0, amount);
                                  } catch (e) {
                                    try {
                                      let yts = require('yt-search');
                                      let { videos } = await yts(search);
                                      console.log(videos[0].url);
                                      return videos.splice(0, amount);
                                    } catch (e) {
                                      try {
                                        let yts = require('yt-search');
                                        let { videos } = await yts(search);
                                        console.log(videos[0].url);
                                        return videos.splice(0, amount);
                                      } catch (e) {
                                        try {
                                          let yts = require('yt-search');
                                          let { videos } = await yts(search);
                                          console.log(videos[0].url);
                                          return videos.splice(0, amount);
                                        } catch (e) {
                                          try {
                                            let yts = require('yt-search');
                                            let { videos } = await yts(search);
                                            console.log(videos[0].url);
                                            return videos.splice(0, amount);
                                          } catch (e) {
                                            try {
                                              let yts = require('yt-search');
                                              let { videos } = await yts(search);
                                              console.log(videos[0].url);
                                              return videos.splice(0, amount);
                                            } catch (e) {
                                              try {
                                                let yts = require('yt-search');
                                                let { videos } = await yts(search);
                                                console.log(videos[0].url);
                                                return videos.splice(0, amount);
                                              } catch (e) {
                                                try {
                                                  let yts = require('yt-search');
                                                  let { videos } = await yts(search);
                                                  console.log(videos[0].url);
                                                  return videos.splice(0, amount);
                                                } catch (e) {
                                                  try {
                                                    let yts = require('yt-search');
                                                    let { videos } = await yts(search);
                                                    console.log(videos[0].url);
                                                    return videos.splice(0, amount);
                                                  } catch (e) {
                                                    try {
                                                      let yts = require('yt-search');
                                                      let { videos } = await yts(search);
                                                      console.log(videos[0].url);
                                                      return videos.splice(0, amount);
                                                    } catch (e) {
                                                      try {
                                                        let yts = require('yt-search');
                                                        let { videos } = await yts(search);
                                                        console.log(videos[0].url);
                                                        return videos.splice(0, amount);
                                                      } catch (e) {
                                                        try {
                                                          let yts = require('yt-search');
                                                          let { videos } = await yts(search);
                                                          console.log(videos[0].url);
                                                          return videos.splice(0, amount);
                                                        } catch (e) {
                                                          let yts = require('yt-search');
                                                          let { videos } = await yts(search);
                                                          return videos.splice(0, amount);
                                                        }
                                                      }
                                                    }
                                                  }
                                                }
                                              }
                                            }
                                          }
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
};
  